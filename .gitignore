# Python
__pycache__/
*.pyc
*.pyo

# Cache de imagens dos herois (baixaveis com build_cache.py / vision.py)
cache/portraits/
cache/icons/

# Temporarios
*.log

# Chave da OpenAI (voz) - server-only, nunca versionar
openai_secret.json

# Historico de relatorios por partida (gerado em tempo de jogo)
match_history/

# Config do overlay (runtime, por maquina)
overlay_config.json
